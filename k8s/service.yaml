apiVersion: v1
kind: Service

metadata:
  name: springboot-service

spec:
  type: NodePort

  selector:
    app: springboot-cicd

  ports:
    - protocol: TCP
      port: 80
      targetPort: 9878
      nodePort: 30080